[project]
name = "qlik-api"
version = "1.0.0"
description = "Minimal encrypt/decrypt API for Qlik extension"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "cryptography>=43.0.0",
]
